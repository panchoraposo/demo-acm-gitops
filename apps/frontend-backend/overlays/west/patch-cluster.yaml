apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: frontend
spec:
  template:
    spec:
      containers:
        - name: checker
          env:
            - name: CLUSTER_NAME
              value: "west"

