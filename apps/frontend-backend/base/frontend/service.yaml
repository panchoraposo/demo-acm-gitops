apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: frontend
spec:
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - name: dummy
      port: 8080
      targetPort: 8080

